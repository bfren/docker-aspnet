name: build

on:
  push:
    branches: main

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout code
        uses: actions/checkout@v2
      -
        name: Read PHP major version
        uses: bencgreen/read-file-action@v1
        with:
          path: ./DOTNET_MAJOR
        id: dotnet_major
      -
        name: Read PHP minor version
        uses: bencgreen/read-file-action@v1
        with:
          path: ./DOTNET_MINOR
        id: dotnet_minor
      -
        name: Read version
        uses: bencgreen/read-file-action@v1
        with:
          path: ./VERSION
        id: version
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to DockerHub
        uses: docker/login-action@v1 
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          push: true
          platforms: linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm64
          tags: |
            bcgdesign/aspnet:latest
            bcgdesign/aspnet:${{ steps.dotnet_major.outputs.content }}
            bcgdesign/aspnet:${{ steps.dotnet_minor.outputs.content }}
            bcgdesign/aspnet:${{ steps.version.outputs.content }}
      -
        name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
